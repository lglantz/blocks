// button specific variables
$button-height = 44px;

$button-primary-background-color = $primary-00;
$button-primary-hover-background-color = $primary-01;
$button-primary-focus-background-color = $primary-01;
$button-primary-active-background-color = $primary-02;

$button-primary-default-border = 2px solid $primary-00;
$button-primary-hover-border = 2px solid $primary-01;
$button-primary-focus-border = 2px solid $focus;
$button-primary-focus-inner-border = 1px solid $secondary-00;
$button-primary-active-border = 2px solid $primary-02;

$button-primary-text-color = $secondary-00;
$button-primary-hover-text-color = $secondary-00;
$button-primary-focus-text-color = $secondary-00;
$button-primary-active-text-color = $secondary-00;

$button-secondary-background-color = $secondary-00;
$button-secondary-hover-background-color = $secondary-01;
$button-secondary-focus-background-color = $secondary-01;
$button-secondary-active-background-color = $secondary-02;

$button-secondary-default-border = 2px solid $secondary-00;
$button-secondary-hover-border = 2px solid $secondary-01;
$button-secondary-focus-border = 2px solid $focus;
$button-secondary-active-border = 2px solid $secondary-02;

$button-secondary-text-color = $primary-00;
$button-secondary-hover-text-color = $primary-00;
$button-secondary-focus-text-color = $primary-00;
$button-secondary-active-text-color = $primary-00;

$button-danger-background-color = $danger-00;
$button-danger-hover-background-color = $danger-01;
$button-danger-focus-background-color = $danger-01;
$button-danger-active-background-color = $danger-02;

$button-danger-default-border = 2px solid $danger-00;
$button-danger-hover-border = 2px solid $danger-01;
$button-danger-focus-border = 2px solid $focus;
$button-danger-focus-inner-border = 1px solid $secondary-00;
$button-danger-active-border = 2px solid $danger-02;

$button-danger-text-color = $secondary-00;
$button-danger-hover-text-color = $secondary-00;
$button-danger-focus-text-color = $secondary-00;
$button-danger-active-text-color = $secondary-00;

$button-dark-background-color = $dark-00;
$button-dark-hover-background-color = $dark-01;
$button-dark-focus-background-color = $dark-01;
$button-dark-active-background-color = $dark-02;

$button-dark-default-border = 2px solid $dark-00;
$button-dark-hover-border = 2px solid $dark-01;
$button-dark-focus-border = 2px solid $focus;
$button-dark-focus-inner-border = 1px solid $secondary-00;
$button-dark-active-border = 2px solid $dark-02;

$button-dark-text-color = $secondary-00;
$button-dark-hover-text-color = $secondary-00;
$button-dark-focus-text-color = $secondary-00;
$button-dark-active-text-color = $secondary-00;

// button styles
.blx-button {
  position: relative;
  display: flex;
  justify-content: center;

  height: $button-height;
  max-width: 480px;
  min-width: 44px;
  margin: 0;
  padding: 8px 18px; // specified padding - 2px to account for border
  text-decoration: none;

  border-radius: $base-border-radius;
  cursor: pointer;

  blxH5();
  line-height: 20px;
  text-align: center;

  box-shadow: $default-box-shadow;

  &.blx-with-margin {
    margin: 12px;
  }

  &:focus, &.blx-focus {
    outline: none;
    text-decoration: none;
  }

  &:hover, &.blx-hover {
    text-decoration: none;
  }

  span {
    blxTruncateText();
    line-height: 20px;
    
    &:not(:first-child) {
      margin-left: 12px;
    }

    &:not(:last-child) {
      margin-right: 12px;
    }
  }
}

.blx-icon-button {
  width: $button-height;
  padding: 11px;
}

.blx-primary {
  background-color: $button-primary-background-color;
  border: $button-primary-default-border;
  color: $button-primary-text-color;

  svg {
    fill: $button-primary-text-color;
  }
  
  &.blx-disabled-rgba {
    background-color: rgba($primary-00-hex, 0.35); // using a var() in an rgba doesn't seem to work
    border: rgba($primary-00-hex, 0.35); // using a var() in an rgba doesn't seem to work
    color: rgba($secondary-00-hex, 0.35); // using a var() in an rgba doesn't seem to work
  }

  &:focus, &.blx-focus {
    border: $button-primary-focus-border;
    background-color: $button-primary-focus-background-color;
    color: $button-primary-focus-text-color;

    &::before {
      content: "";
      position: absolute;
      left: 0;
      top: 0;
      border-radius: 5px;
      width: calc(100% - 2px);
      height: calc(100% - 2px);
      border: $button-primary-focus-inner-border;
    }
  }

  &:hover, &.blx-hover {
    background-color: $button-primary-hover-background-color;
    border: $button-primary-hover-border;
    color: $button-primary-hover-text-color;

    &::before {
      content: none;
    }
  }

  &:active, &.blx-active {
    background-color: $button-primary-active-background-color;
    border: $button-primary-active-border;
    color: $button-primary-active-text-color;
    &::before {
      content: none;
    }
  }
}

.blx-secondary {
  background-color: $button-secondary-background-color;
  border: $button-secondary-default-border;
  color: $button-secondary-text-color;

  svg {
    fill: $button-secondary-text-color;
  }
  
  &.blx-disabled-rgba {
    background-color: rgba($secondary-00-hex, 0.35); // using a var() in an rgba doesn't seem to work
    border: rgba($secondary-00-hex, 0.35); // using a var() in an rgba doesn't seem to work
    color: rgba($primary-00-hex, 0.35); // using a var() in an rgba doesn't seem to work
  }

  &:focus, &.blx-focus {
    border: $button-secondary-focus-border;
    background-color: $button-secondary-focus-background-color;
    color: $button-secondary-focus-text-color;
  }

  &:hover, &.blx-hover {
    background-color: $button-secondary-hover-background-color;
    border: $button-secondary-hover-border;
    color: $button-secondary-hover-text-color;

    &::before {
      content: none;
    }
  }

  &:active, &.blx-active {
    background-color: $button-secondary-active-background-color;
    border: $button-secondary-active-border;
    color: $button-secondary-active-text-color;
    &::before {
      content: none;
    }
  }
}

.blx-danger {
  background-color: $button-danger-background-color;
  border: $button-danger-default-border;
  color: $button-danger-text-color;

  svg {
    fill: $button-danger-text-color;
  }

  &:focus, &.blx-focus {
    border: $button-danger-focus-border;
    background-color: $button-danger-focus-background-color;
    color: $button-danger-focus-text-color;

    &::before {
      content: "";
      position: absolute;
      left: 0;
      top: 0;
      border-radius: 5px;
      width: calc(100% - 2px);
      height: calc(100% - 2px);
      border: $button-danger-focus-inner-border
    }

    svg {
      fill: $button-danger-hover-text-color;
    }
  }

  &:hover, &.blx-hover {
    background-color: $button-danger-hover-background-color;
    border: $button-danger-hover-border;
    color: $button-danger-hover-text-color;

    &::before {
      content: none;
    }

    svg {
      fill: $button-danger-hover-text-color;
    }
  }

  &:active, &.blx-active {
    background-color: $button-danger-active-background-color;
    border: $button-danger-active-border;
    color: $button-danger-active-text-color;
    &::before {
      content: none;
    }

    svg {
      fill: $button-danger-hover-text-color;
    }
  }
}

.blx-dark {
  background-color: $button-dark-background-color;
  border: $button-dark-default-border;
  color: $button-dark-text-color;

  &.blx-disabled-rgba {
    background-color: rgba($dark-00-hex, 0.35); // using a var() in an rgba doesn't seem to work
    border: rgba($dark-00-hex, 0.35); // using a var() in an rgba doesn't seem to work
    color: rgba($secondary-00-hex, 0.35); // using a var() in an rgba doesn't seem to work
  }

  .blx-icon {
    background-color: $button-dark-text-color;
  }

  &:focus, &.blx-focus {
    border: $button-dark-focus-border;
    background-color: $button-dark-focus-background-color;
    color: $button-dark-focus-text-color;

    &::before {
      content: "";
      position: absolute;
      left: 0;
      top: 0;
      border-radius: 5px;
      width: calc(100% - 2px);
      height: calc(100% - 2px);
      border: $button-dark-focus-inner-border
    }

    .blx-icon {
      background-color: $button-dark-hover-text-color;
    }
  }

  &:hover, &.blx-hover {
    background-color: $button-dark-hover-background-color;
    border: $button-dark-hover-border;
    color: $button-dark-hover-text-color;

    &::before {
      content: none;
    }

    .blx-icon {
      background-color: $button-dark-hover-text-color;
    }
  }

  &:active, &.blx-active {
    background-color: $button-dark-active-background-color;
    border: $button-dark-active-border;
    color: $button-dark-active-text-color;
    &::before {
      content: none;
    }

    .blx-icon {
      background-color: $button-dark-hover-text-color;
    }
  }
}

.blx-loading-btn {
  .blx-icon {
    background-color: transparent;
    border: 0px;
  }
}

a.blx-button {
  display: inline-flex;
  align-items: center;
}

.blx-button-group {
  .blx-button {
    margin-right: 10px;
    margin-bottom: 12px;

    &:last-child {
      margin-right: 0;
    }
  }
}

.blx-h-button-group {
  display: flex;
  flex-direction: row;
  min-height: $button-height + $default-shadow-bottom-height;

  .blx-button  {
    margin-left: 0;
    margin-right: 10px;

    &:first-child {
      margin-left: 20px;
    }

    &:last-child {
      margin-right: 20px;
    }
  }
}

.blx-v-button-group {
  display: flex;
  flex-direction: column;

  .blx-button {
    margin-top: 0;
    margin-bottom: 12px;
    margin-left: 10px;
    margin-right: 10px;

    &:first-child {
      margin-top: 24px;
    }

    &:last-child {
      margin-bottom: 24px;
    }
  }
}
