//
// STRUCTURE
//

.loading {
  position: absolute;
  top: calc(50% - 100px);
  left: calc(50% - 100px);
  animation: blink 3s infinite;
}

.load-error {
  position: relative;
}

@keyframes blink {
  0% {opacity: 0;}
  50% {opacity: 1;}
  100% {opacity: 0;}
}

//
// THEME
//
